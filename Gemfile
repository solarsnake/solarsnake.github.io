source "https://rubygems.org"

# Specify Jekyll version
gem "jekyll", "~> 4.3.3" # Or the latest stable version you are using (4.3.4 based on logs)

# CSV, Logger, and Base64 gems needed for Ruby 3.4+
gem "csv"
gem "logger"
gem "base64"

# Default theme (as specified in your _config.yml)
gem "minima", "~> 2.5"

# Jekyll plugins (add any others you plan to use)
gem "jekyll-feed", "~> 0.12" # For an RSS feed, as in your _config.yml

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
# gem "tzinfo-data", platforms: [:mingw, :mswin, :x64_mingw, :jruby] # Uncomment if on Windows

# If you have other plugins, add them here:
gem "jekyll-paginate"
